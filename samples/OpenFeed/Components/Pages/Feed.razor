@page "/feed"
@using AvantiPoint.Packages.UI.Components
@rendermode @(new Microsoft.AspNetCore.Components.Web.InteractiveServerRenderMode())

<FeedInfo FeedName="OpenFeed" ShowAuthenticationInfo="false" />

@code {
    // Dedicated page for connection details; kept minimal intentionally.
}